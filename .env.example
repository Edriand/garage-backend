# AWS Configuration
# These are automatically picked up by CDK from your AWS CLI configuration
# Only set these if you need to override the defaults
# CDK_DEFAULT_ACCOUNT=123456789012
# CDK_DEFAULT_REGION=us-east-1

# Environment (dev, staging, prod)
ENVIRONMENT=dev

# Optional: Custom stack name prefix
# STACK_PREFIX=MyGarage
